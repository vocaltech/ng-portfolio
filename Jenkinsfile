pipeline {
    agent any
    stages {
        stage("Build") {
            steps {
                echo "Building.."
                sh '''
                    echo "doing build stuff.."
                '''
            }
        }
    }
}